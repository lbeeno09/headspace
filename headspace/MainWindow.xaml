<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="headspace.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:headspace"
    xmlns:viewes="using:headspace.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Head Space">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Menu bar -->
            <RowDefinition Height="Auto" /> <!-- NagivationView -->
            <RowDefinition Height="*" /> <!-- Page content -->
        </Grid.RowDefinitions>

        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New" />
                <MenuFlyoutItem Text="Open..." />
                <MenuFlyoutItem Text="Save" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" />
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutItem Text="Undo" />
                <MenuFlyoutItem Text="Redo" />
                <MenuFlyoutItem Text="Cut" />
                <MenuFlyoutItem Text="Copy" />
                <MenuFlyoutItem Text="Paste" />
            </MenuBarItem>
            <MenuBarItem Title="View">
                <MenuFlyoutItem Text="Zoom In" />
                <MenuFlyoutItem Text="Zoom Out" />
            </MenuBarItem>
            <MenuBarItem Title="Window">
                <MenuFlyoutItem Text="Minimize" />
                <MenuFlyoutItem Text="Mazimize" />
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About" />
                <MenuFlyoutItem Text="Documentation" />
            </MenuBarItem>
        </MenuBar>
        
        <NavigationView
            Grid.Row="1"
            x:Name="RootNavigationView"
            ItemInvoked="NavView_ItemInvoked"
            IsBackButtonVisible="Collapsed"
            IsPaneOpen="False"
            IsPaneToggleButtonVisible="True"
            IsSettingsVisible="False"
            Loaded="NavView_Loaded"
            PaneDisplayMode="Top">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Tag="HomePage" Icon="Home" />
                <NavigationViewItem Content="Notes" Tag="NotesPage" Icon="Page" />
                <NavigationViewItem Content="Documents" Tag="DocumentsPage" Icon="Document" />
                <NavigationViewItem Content="Screen Play" Tag="ScreenplayPage" Icon="Document" />
                <NavigationViewItem Content="Drawings" Tag="DrawingsPage" Icon="Edit" />
                <NavigationViewItem Content="Moodboard" Tag="MoodboardPage" Icon="Bookmarks" />
                <NavigationViewItem Content="Storyboard" Tag="StoryboardPage" Icon="PhoneBook" />
                <NavigationViewItem Content="Music" Tag="MusicPage" Icon="MusicInfo" />
            </NavigationView.MenuItems>
        </NavigationView>

        <Frame Grid.Row="2" x:Name="ContentFrame" />
    </Grid>
</Window>
