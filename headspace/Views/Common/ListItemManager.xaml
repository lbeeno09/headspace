<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="headspace.Views.Common.ListItemManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:headspace.Views.Common"
    mc:Ignorable="d">
    <StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Command="{Binding AddCommand}">
                <SymbolIcon Symbol="Add" />
            </Button>
            <Button Command="{Binding DeleteCommand}">
                <SymbolIcon Symbol="Delete" />
            </Button>
            <Button Command="{Binding RenameCommand}">
                <SymbolIcon Symbol="Rename" />
            </Button>
        </StackPanel>

        <ListView ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  SelectionMode="Single"
                  Background="Transparent"
                  HorizontalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
