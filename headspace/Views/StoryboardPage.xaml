<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="headspace.Views.StoryboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:headspace.Views"
    xmlns:vm="using:headspace.ViewModels"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:StoryboardViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <!-- Storyboard List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Command="{Binding AddStoryboardCommand}">
                    <SymbolIcon Symbol="Add" />
                </Button>
                <Button Command="{Binding DeleteStoryboardCommand}">
                    <SymbolIcon Symbol="Remove" />
                </Button>
                <Button Click="RenameButton_Click">
                    <SymbolIcon Symbol="Rename" />
                </Button>
            </StackPanel>

            <ListBox
                Grid.Row="1"
                ItemsSource="{Binding Storyboards}"
                SelectedItem="{Binding SelectedStoryboard, Mode=TwoWay}"
                DisplayMemberPath="Title" />
        </Grid>

        <Border Grid.Column="1" Width="10" />

        <!-- Main Window -->
        <Grid Grid.Column="2" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <StackPanel Orientation="Horizontal" Spacing="10">
                <ComboBox
                    ItemsSource="{Binding ColorOptions}"
                    SelectedValue="{Binding SelectedNamedColor, Mode=TwoWay}" 
                    Width="100" />

                <Slider
                    Value="{Binding SelectedThickness, Mode=TwoWay}"
                    Width="100"
                    Minimum="1" Maximum="10" />

                <ToggleButton
                    Content="Eraser"
                    IsChecked="{Binding IsEraserMode, Mode=TwoWay}" />
            </StackPanel>

            <!-- Drawing Area -->
            <Canvas
                x:Name="DrawingCanvas"
                Grid.Row="1"
                Background="White"
                PointerPressed="Canvas_PointerPressed"
                PointerMoved="Canvas_PointerMoved"
                PointerReleased="Canvas_PointerReleased" />
        </Grid>
    </Grid>
</Page>
