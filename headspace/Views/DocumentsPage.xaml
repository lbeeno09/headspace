<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="headspace.Views.DocumentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:headspace.Views"
    xmlns:vm="using:headspace.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:DocumentViewModel x:Name="ViewModel" />
    </Page.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <!-- Document List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Command="{Binding AddDocumentCommand}">
                    <SymbolIcon Symbol="Add" />
                </Button>
                <Button Command="{Binding DeleteDocumentCommand}">
                    <SymbolIcon Symbol="Remove" />
                </Button>
                <Button Click="RenameButton_Click">
                    <SymbolIcon Symbol="Rename" />
                </Button>
            </StackPanel>

            <ListBox
                Grid.Row="1"
                ItemsSource="{Binding Documents}"
                SelectedItem="{Binding SelectedDocument, Mode=TwoWay}"
                DisplayMemberPath="Title" />
        </Grid>

        <Border Grid.Column="1" Width="10" />

        <!-- Main Window -->
        <Grid Grid.Column="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <!-- Tool bar -->
            <StackPanel Orientation="Horizontal" Spacing="10">
                <ToggleButton x:Name="BoldButton" Content="B" Click="BoldButton_Click" />
                <ToggleButton x:Name="ItalicButton" Content="I" Click="ItalicButton_Click" />
                <ToggleButton x:Name="UnderlineButton" Content="U" Click="UnderlineButton_Click" />
            </StackPanel>

            <!-- Rich Edit Box -->
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,20">
                    <Border
                        Background="White"
                        Width="800"
                        Height="1131"
                        Padding="75"
                        CornerRadius="2"
                        BorderBrush="Gray"
                        BorderThickness="1">
                        <RichEditBox
                            x:Name="Editor"                            
                            IsEnabled="{Binding IsItemSelected}"
                            AcceptsReturn="True"
                            TextWrapping="Wrap"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            BorderThickness="0"
                            Foreground="Black"
                            Background="White"
                            KeyDown="Editor_KeyDown">
                            <RichEditBox.Resources>
                                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent" />
                                <SolidColorBrush x:Key="TextControlForegroundFocused" Color="Black" />
                            </RichEditBox.Resources>
                        </RichEditBox>
                    </Border>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
